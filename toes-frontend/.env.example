# The API base URL when the frontend is deployed separately.
# In production, set this to your Render backend URL.
# Example: https://toes-backend.onrender.com
VITE_API_BASE_URL=http://localhost:3001
